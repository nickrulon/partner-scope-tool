node_modules/
client/dist/
.env

# Local data — these live on the persistent volume in production
server/scope-tool.db
server/uploads/
server/outputs/

# macOS Finder metadata
.DS_Store

# goose-game runtime data (accounts/purchases live on a persistent volume in prod)
goose-game/data/
